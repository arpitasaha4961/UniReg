json.extract! rail, :id, :generate, :scaffold, :StudentProfile, :user_id, :first_name, :last_name, :date_of_birth, :address, :phone, :email, :emergency_contact_name, :emergency_contact_phone, :created_at, :updated_at
json.url rail_url(rail, format: :json)
