<p id="notice"><%= notice %></p>

<p>
  <strong>Title:</strong>
  <%= @course.title %>
</p>

<p>
  <strong>Course code:</strong>
  <%= @course.course_code %>
</p>

<p>
  <strong>Credit:</strong>
  <%= @course.credit %>
</p>

<p>
  <strong>Semester:</strong>
  <%= @course.semester %>
</p>
<% if current_user.role == 'student' %>
  <td><%= button_to "Enroll", courses_enroll_path(c_id: @course.id), method: :post %></td>
<% end %>
<%# if current_user.student? && !@course.enrolled?(current_user) %>
  <%#= link_to 'Enroll in this course', enroll_course_path(@course), method: :post, class: 'btn btn-primary' %>
<%# end %>
<%#= button_to "Enroll in Course", enroll_course_path(@course), method: :post %>
<% if current_user.role == 'admin' %>
  <%= link_to 'Edit', edit_course_path(@course) %> |
  <%= link_to 'Back', courses_path %>
<% end %>

